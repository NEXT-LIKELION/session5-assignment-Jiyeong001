for (let i = 1; i <= 9; i += 2) {
    let line = "";

    for (let j = 0; j < (9 - i) / 2; j++) {
        line += " ";
    }

    for (let k = 0; k < i; k++) {
        line += "*";
    }

    console.log(line); 
}
